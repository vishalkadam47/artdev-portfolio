{
    "buildCommand": "corepack enable && corepack prepare pnpm@10.11.0 --activate && pnpm build",
    "installCommand": "corepack enable && corepack prepare pnpm@10.11.0 --activate && pnpm install",
    "framework": "nextjs",
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/next"
        }
    ]
} 